# frozen_string_literal: true

source "https://rubygems.org"

# Add your gems here

gem "jekyll", "~> 3.9.0" # Update to a version compatible with github-pages
gem "jekyll-avatar", "~> 0.7.0"
gem "github-pages", "~> 211"

# Specify a compatible version of eventmachine
gem "eventmachine", "1.0.9.1"

# Group for development and test gems
group :development, :test do
  # Add gems used only for development or testing
end
